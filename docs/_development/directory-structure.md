```
polar-visualizer/
├── index.html
├── src/
│   ├── main.ts              # Entry point: initializes UI & canvas
│   ├── canvas.ts            # Geometry, drawing, color mapping
│   ├── audio.ts             # Web Audio analyzer logic
│   ├── utils.ts             # Math helpers (log-freq, dB mapping)
│   └── types.ts             # Shared interfaces (PolarCell, etc.)
├── tsconfig.json
├── vite.config.ts
└── package.json
```
