// animation.constants.ts

// Smoothstep easing (t² * (A - B*t))
export const SMOOTHSTEP_A = 3;
export const SMOOTHSTEP_B = 2;

// Interpolation clamp bounds
export const INTERP_MIN = 0;
export const INTERP_MAX = 1;

// Default rotation animation duration
export const ROTATION_DURATION_MS = 500;
