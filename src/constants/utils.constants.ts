// utils.constants.ts

// Minimum frequency for log-spaced visualization
export const FREQ_MIN_HZ = 20;

// Nyquist divisor (sampleRate / 2)
export const NYQUIST_DIVISOR = 2;

// Normalization range (dB: -60..0)
export const NORMALIZE_DB_RANGE = 60;
